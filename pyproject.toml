[project]
name = "rfrl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "rfrl-gym",
    "scipy-stubs~=1.18.0",
]

#[project.scripts]
#rfrl = "rfrl:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"

[tool.uv.workspace]
members = [
    "rfrl-gym",
]

[tool.uv.sources]
rfrl-gym = { workspace = true, editable = true }
